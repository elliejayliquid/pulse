# Pulse - AI companion heartbeat daemon
openai>=1.0.0                   # llama-server API client (OpenAI-compatible)
anthropic>=0.40                 # Native Anthropic API client (optional, for provider: anthropic)
httpx>=0.27                     # Health checks for llama-server
pyyaml>=6.0                     # Config file parsing
python-dotenv>=1.0              # Load .env secrets
win11toast>=0.34                # Windows desktop notifications (optional, Windows only)
python-telegram-bot>=21.0       # Telegram bot (Phase 2)
sentence-transformers>=2.0      # Memory embeddings
numpy>=1.24                     # Vector operations for memory/journal search
ddgs>=9.0                       # Web search skill (DuckDuckGo)
pillow>=10.0                    # Image rendering for paint skill (pixel art → PNG)

# Note: plyer is an alternative to win11toast for cross-platform
# pip install plyer
